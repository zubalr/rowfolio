{
  "name": "@rowfolio/ingest-tests",
  "private": true,
  "type": "module",
  "description": "Unit suite for @rowfolio/ingest — bounded ZIP preflight, CSV/XLSX adapters, typed failures, oracle reconciliation. Not a workspace member (tests/* is outside pnpm-workspace globs); root `pnpm test` picks these files up via vitest.config.ts.",
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
